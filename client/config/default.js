const config = {
  MEDIA_SERVER: {
    HTTP: {
        URL: 'media_server_url',
        WEB_RTC_PORT: 3333,
        LL_DASH_PORT: 8080,
        LL_DASH_PORT_SEC: 8081,
    },
  }
};

module.exports = config;